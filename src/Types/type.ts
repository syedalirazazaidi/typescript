export interface newItemType {
  id: number;
  title: string;
}
export interface newType {
  list: newItemType;
}
export interface alerType {
  show: boolean;
  msg: string;
  type: string;
}

export interface newType {
  id: number;
  title: string;
}
